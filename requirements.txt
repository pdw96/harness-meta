# harness-meta runtime dependencies
# Shell/Python automation in this repo uses stdlib only — no pip install needed.
# Python scripts live under skills/ and scripts/.
#
# Dev tooling (optional):
# shellcheck     # shell linter — install via: brew install shellcheck / apt install shellcheck
# bats-core      # bash test framework (alternative to custom smoke scripts)
